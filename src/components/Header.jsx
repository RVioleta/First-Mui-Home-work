import { Info } from "@mui/icons-material";
import {Typography, Box} from "@mui/material";



const Header = () => {
    return (
        <Box style={{marginBottom:50, marginTop:50}}>
            <Typography variant="h2" >My first web page in React</Typography>
        </Box>
    )
};

export default Header;